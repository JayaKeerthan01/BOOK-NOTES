<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sedan:ital@0;1&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
    <title>BOOK NOTES</title>
</head>
<body>
    <div class="title">
        <div class="head">
            <h1><a href="/">BOOK NOTES</a></h1>
            <h5><a href="/add_new">Add New BOOK +</a></h5>
        </div>
        <h2>BOOKS I'VE READ</h2>
        <p>Tiny summary but detailed notes for each. Use the ISBN number to find it from your local library or anywhere else. This page will constantly
             update as I read more, so bookmark it if you want to check back in a few months.
        </p>
        <hr>
    </div>
    <section>
        <% books.forEach((book)=>{ %>
        <div class="book_details">
            <div class="book_cover">
                <div class="image">
                    <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-M.jpg" alt="">
                </div>
                <div class="text">
                    <h1><a href="/read_notes/<%= book.id %>"><%= book.title %></a></h1>
                    <h5><b>Date read: <% `${new Date().getFullYear()}-${new Date().getMonth()}-${new Date().getDate()}` %>. How strongly I recommend it: <%= book.rating %>/10</b></h5>
                    <p><%= book.description %></p>
                </div>
            </div>
            <a href="/read_notes/<%= book.id %>">Read my notes</a> 
            <a href="/delete_book/<%= book.id %>" class="delete_button">Delete Book</a>
            <a href="/edit_book/<%= book.id %>" class="edit_button">Edit Book</a>
        </div>
        <% }) %>
    </section>
<!-- <script>
    const date=new Date()
    const today=document.getElementById('date').innerHTML=`${date.getFullYear()}-${date.getMonth()}-${date.getDate()}`
</script> -->
    
</body>
</html>